// Cookie names for the active child profile + play session.
// httpOnly, set server-side only. The kid world (Phase 3+) reads these.
export const SKY_CHILD_COOKIE = 'sky_child';
export const SKY_SESSION_COOKIE = 'sky_session';
